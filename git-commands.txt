git status -----to check status
git init   ------to convert normal folder to git local repo   .git  hidden folder will get created 

git add . ----to add untracked fils to local repo
git commit -m "message " -----commit the chages  are get commited 

git log -----  to check all changes


git checkout timeineid  --- to get

git checkout master --- to come back at current timeline

git remote add origin "link"  ------to connect local repo too remote repo
git remote ------ to check is it connected
git push origin master   --------to push 